export const Role = {
  USER: 'user',
  MODEL: 'model',
};

// Definindo ChatMessage como um tipo de objeto para uso em JS
// As interfaces TypeScript são removidas, mas mantemos a estrutura para o JS
// O ChatMessage é usado apenas como um objeto de dados no JS
// DatabaseQueryTool não é usado diretamente no JS, mas mantemos a exportação para evitar erros de referência
export const ChatMessage = {}; 
export const DatabaseQueryTool = {};